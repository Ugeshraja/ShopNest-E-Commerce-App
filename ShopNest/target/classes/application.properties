# Database Configuration for MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/shopnest?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Ugesh@007
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA and Hibernate Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server Port
server.port=8080

# Spring Security Logging (Optional debug helper)
# logging.level.org.springframework.security=DEBUG

# Enable Thymeleaf Cache in Production? Set to false for development so changes reflect immediately
spring.thymeleaf.cache=false
